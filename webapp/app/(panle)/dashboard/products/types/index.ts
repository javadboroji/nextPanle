export type createProduct = {
    title: string,
    count: number,
    price: number,
    barcode: number,
    categoryId?: number,
    code?: number,
    discontPrice?: number
    model?: string,
    quantity: number,
    status: string,
    tagId?: number,
    description?: string,
}